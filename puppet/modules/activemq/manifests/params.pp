class activemq::params {
    $version = "5.6.0"
    $service_name = "activemq-${version}"   
    $package_name = "apache-activemq-${version}-bin.tar.gz"
    $install_dir = "/opt/activemq-${version}"
}
